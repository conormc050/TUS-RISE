# Roadmap (initial)

1. Prototype CV pipeline (pose estimation -> spinal metrics)
2. Create dataset of labeled safe/unsafe reps
3. Implement local iOS inference or low-latency server inference
4. Build UX for actionable feedback (e.g., "Keep back neutral", "Hips too high")
5. Testing & user studies
